import styled from "styled-components";

import { AddCountry } from "../types";

interface Props {
  addCountry: AddCountry;
}

function Add({ addCountry }: Props): JSX.Element {
  return <div>Add Page</div>;
}

export default Add;
